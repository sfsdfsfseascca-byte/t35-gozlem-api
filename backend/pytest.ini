[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    live: tests that require network access (deselected unless EH_RUN_LIVE_TESTS=1)
